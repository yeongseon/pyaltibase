services:
  db:
    image: ${ALTIBASE_IMAGE:-altibase/a_plus_edition:latest}
    hostname: ${ALTIBASE_HOSTNAME:-altibase}
    ports:
      - "${ALTIBASE_PORT:-20300}:20300"
    environment:
      MODE: foreground
      DB_SIZE: "${ALTIBASE_DB_SIZE:-500}"
      DB_CHARSET: "${ALTIBASE_DB_CHARSET:-UTF8}"
      NATIONAL_CHARSET: "${ALTIBASE_NATIONAL_CHARSET:-UTF8}"

  e2e:
    build:
      context: .
      dockerfile: docker/e2e.Dockerfile
      args:
        BASE_IMAGE: ${ALTIBASE_IMAGE:-altibase/a_plus_edition:latest}
    depends_on:
      - db
    environment:
      PYALTIBASE_RUN_E2E: "1"
      PYALTIBASE_HOST: db
      PYALTIBASE_PORT: "${ALTIBASE_PORT:-20300}"
      PYALTIBASE_DATABASE: ${ALTIBASE_DATABASE:-mydb}
      PYALTIBASE_USER: ${ALTIBASE_USER:-sys}
      PYALTIBASE_PASSWORD: ${ALTIBASE_PASSWORD:-manager}
      PYALTIBASE_DRIVER: ${ALTIBASE_DRIVER:-ALTIBASE_HDB_ODBC_64bit}
      PYALTIBASE_NLS_USE: ${ALTIBASE_NLS_USE:-UTF8}

